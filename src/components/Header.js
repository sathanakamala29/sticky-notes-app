import React from 'react';
import './Header.css'; // Assuming you have a separate CSS file for the header styles

const Header = () => (
  <header>
    <h1 className="title">
      <img src='./purple.jpg' />
      Note Keeper
    </h1>
  </header>
);

export default Header;

